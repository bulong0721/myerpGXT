package org.adempiere.model;

import java.io.*;
import java.util.*;

/**
 * Application identity class for: org.adempiere.model.BBidcomment
 *
 * Auto-generated by:
 * org.apache.openjpa.enhance.ApplicationIdTool
 */
public class BBidcommentPK implements Serializable {
	static {
		// register persistent class in JVM
		try { Class.forName("org.adempiere.model.BBidcomment"); }
		catch(Exception e) {}
	}

	public Integer bBidcommentId;
	public Integer bTopicId;

	public BBidcommentPK() {
	}

	public BBidcommentPK(String str) {
		fromString(str);
	}

	public Integer getBBidcommentId() {
		return bBidcommentId;
	}

	public void setBBidcommentId(Integer bBidcommentId) {
		this.bBidcommentId = bBidcommentId;
	}

	public Integer getBTopicId() {
		return bTopicId;
	}

	public void setBTopicId(Integer bTopicId) {
		this.bTopicId = bTopicId;
	}

	public String toString() {
		return String.valueOf(bBidcommentId)
			+ "::" + String.valueOf(bTopicId);
	}

	public int hashCode() {
		int rs = 17;
		rs = rs * 37 + ((bBidcommentId == null) ? 0 : bBidcommentId.hashCode());
		rs = rs * 37 + ((bTopicId == null) ? 0 : bTopicId.hashCode());
		return rs;
	}

	public boolean equals(Object obj) {
		if(this == obj)
			return true;
		if(obj == null || obj.getClass() != getClass())
			return false;

		BBidcommentPK other = (BBidcommentPK) obj;
		return ((bBidcommentId == null && other.bBidcommentId == null)
			|| (bBidcommentId != null && bBidcommentId.equals(other.bBidcommentId)))
			&& ((bTopicId == null && other.bTopicId == null)
			|| (bTopicId != null && bTopicId.equals(other.bTopicId)));
	}

	private void fromString(String str) {
		Tokenizer toke = new Tokenizer(str);
		str = toke.nextToken();
		if("null".equals(str))
			bBidcommentId = null;
		else
			bBidcommentId = new Integer(str);
		str = toke.nextToken();
		if("null".equals(str))
			bTopicId = null;
		else
			bTopicId = new Integer(str);
	}

	protected static class Tokenizer {
		private final String str;
		private int last;

		public Tokenizer (String str) {
			this.str = str;
		}

		public String nextToken () {
			int next = str.indexOf("::", last);
			String part;
			if(next == -1) {
				part = str.substring(last);
				last = str.length();
			} else {
				part = str.substring(last, next);
				last = next + 2;
			}
			return part;
		}
	}
}