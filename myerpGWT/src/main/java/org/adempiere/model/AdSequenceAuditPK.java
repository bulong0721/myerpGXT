package org.adempiere.model;

import java.io.*;
import java.util.*;

/**
 * Application identity class for: org.adempiere.model.AdSequenceAudit
 *
 * Auto-generated by:
 * org.apache.openjpa.enhance.ApplicationIdTool
 */
public class AdSequenceAuditPK implements Serializable {
	static {
		// register persistent class in JVM
		try { Class.forName("org.adempiere.model.AdSequenceAudit"); }
		catch(Exception e) {}
	}

	public Integer adSequenceId;
	public String documentno;

	public AdSequenceAuditPK() {
	}

	public AdSequenceAuditPK(String str) {
		fromString(str);
	}

	public Integer getAdSequenceId() {
		return adSequenceId;
	}

	public void setAdSequenceId(Integer adSequenceId) {
		this.adSequenceId = adSequenceId;
	}

	public String getDocumentno() {
		return documentno;
	}

	public void setDocumentno(String documentno) {
		this.documentno = documentno;
	}

	public String toString() {
		return String.valueOf(adSequenceId)
			+ "::" + documentno;
	}

	public int hashCode() {
		int rs = 17;
		rs = rs * 37 + ((adSequenceId == null) ? 0 : adSequenceId.hashCode());
		rs = rs * 37 + ((documentno == null) ? 0 : documentno.hashCode());
		return rs;
	}

	public boolean equals(Object obj) {
		if(this == obj)
			return true;
		if(obj == null || obj.getClass() != getClass())
			return false;

		AdSequenceAuditPK other = (AdSequenceAuditPK) obj;
		return ((adSequenceId == null && other.adSequenceId == null)
			|| (adSequenceId != null && adSequenceId.equals(other.adSequenceId)))
			&& ((documentno == null && other.documentno == null)
			|| (documentno != null && documentno.equals(other.documentno)));
	}

	private void fromString(String str) {
		Tokenizer toke = new Tokenizer(str);
		str = toke.nextToken();
		if("null".equals(str))
			adSequenceId = null;
		else
			adSequenceId = new Integer(str);
		str = toke.nextToken();
		if("null".equals(str))
			documentno = null;
		else
			documentno = str;
	}

	protected static class Tokenizer {
		private final String str;
		private int last;

		public Tokenizer (String str) {
			this.str = str;
		}

		public String nextToken () {
			int next = str.indexOf("::", last);
			String part;
			if(next == -1) {
				part = str.substring(last);
				last = str.length();
			} else {
				part = str.substring(last, next);
				last = next + 2;
			}
			return part;
		}
	}
}