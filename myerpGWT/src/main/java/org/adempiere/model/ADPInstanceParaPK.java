package org.adempiere.model;

import java.io.*;
import java.util.*;

/**
 * Application identity class for: org.adempiere.model.ADPInstancePara
 *
 * Auto-generated by:
 * org.apache.openjpa.enhance.ApplicationIdTool
 */
public class ADPInstanceParaPK implements Serializable {
	static {
		// register persistent class in JVM
		try { Class.forName("org.adempiere.model.ADPInstancePara"); }
		catch(Exception e) {}
	}

	public Integer aDPInstanceID;
	public Integer seqNo;

	public ADPInstanceParaPK() {
	}

	public ADPInstanceParaPK(String str) {
		fromString(str);
	}

	public Integer getADPInstanceID() {
		return aDPInstanceID;
	}

	public void setADPInstanceID(Integer aDPInstanceID) {
		this.aDPInstanceID = aDPInstanceID;
	}

	public Integer getSeqNo() {
		return seqNo;
	}

	public void setSeqNo(Integer seqNo) {
		this.seqNo = seqNo;
	}

	public String toString() {
		return String.valueOf(aDPInstanceID)
			+ "::" + String.valueOf(seqNo);
	}

	public int hashCode() {
		int rs = 17;
		rs = rs * 37 + ((aDPInstanceID == null) ? 0 : aDPInstanceID.hashCode());
		rs = rs * 37 + ((seqNo == null) ? 0 : seqNo.hashCode());
		return rs;
	}

	public boolean equals(Object obj) {
		if(this == obj)
			return true;
		if(obj == null || obj.getClass() != getClass())
			return false;

		ADPInstanceParaPK other = (ADPInstanceParaPK) obj;
		return ((aDPInstanceID == null && other.aDPInstanceID == null)
			|| (aDPInstanceID != null && aDPInstanceID.equals(other.aDPInstanceID)))
			&& ((seqNo == null && other.seqNo == null)
			|| (seqNo != null && seqNo.equals(other.seqNo)));
	}

	private void fromString(String str) {
		Tokenizer toke = new Tokenizer(str);
		str = toke.nextToken();
		if("null".equals(str))
			aDPInstanceID = null;
		else
			aDPInstanceID = new Integer(str);
		str = toke.nextToken();
		if("null".equals(str))
			seqNo = null;
		else
			seqNo = new Integer(str);
	}

	protected static class Tokenizer {
		private final String str;
		private int last;

		public Tokenizer (String str) {
			this.str = str;
		}

		public String nextToken () {
			int next = str.indexOf("::", last);
			String part;
			if(next == -1) {
				part = str.substring(last);
				last = str.length();
			} else {
				part = str.substring(last, next);
				last = next + 2;
			}
			return part;
		}
	}
}