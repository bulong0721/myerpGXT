package org.adempiere.model;

import java.io.*;
import java.util.*;

/**
 * Application identity class for: org.adempiere.model.AdPrivateAccess
 *
 * Auto-generated by:
 * org.apache.openjpa.enhance.ApplicationIdTool
 */
public class AdPrivateAccessPK implements Serializable {
	static {
		// register persistent class in JVM
		try { Class.forName("org.adempiere.model.AdPrivateAccess"); }
		catch(Exception e) {}
	}

	public Integer adTableId;
	public Integer adUserId;
	public Integer recordId;

	public AdPrivateAccessPK() {
	}

	public AdPrivateAccessPK(String str) {
		fromString(str);
	}

	public Integer getAdTableId() {
		return adTableId;
	}

	public void setAdTableId(Integer adTableId) {
		this.adTableId = adTableId;
	}

	public Integer getAdUserId() {
		return adUserId;
	}

	public void setAdUserId(Integer adUserId) {
		this.adUserId = adUserId;
	}

	public Integer getRecordId() {
		return recordId;
	}

	public void setRecordId(Integer recordId) {
		this.recordId = recordId;
	}

	public String toString() {
		return String.valueOf(adTableId)
			+ "::" + String.valueOf(adUserId)
			+ "::" + String.valueOf(recordId);
	}

	public int hashCode() {
		int rs = 17;
		rs = rs * 37 + ((adTableId == null) ? 0 : adTableId.hashCode());
		rs = rs * 37 + ((adUserId == null) ? 0 : adUserId.hashCode());
		rs = rs * 37 + ((recordId == null) ? 0 : recordId.hashCode());
		return rs;
	}

	public boolean equals(Object obj) {
		if(this == obj)
			return true;
		if(obj == null || obj.getClass() != getClass())
			return false;

		AdPrivateAccessPK other = (AdPrivateAccessPK) obj;
		return ((adTableId == null && other.adTableId == null)
			|| (adTableId != null && adTableId.equals(other.adTableId)))
			&& ((adUserId == null && other.adUserId == null)
			|| (adUserId != null && adUserId.equals(other.adUserId)))
			&& ((recordId == null && other.recordId == null)
			|| (recordId != null && recordId.equals(other.recordId)));
	}

	private void fromString(String str) {
		Tokenizer toke = new Tokenizer(str);
		str = toke.nextToken();
		if("null".equals(str))
			adTableId = null;
		else
			adTableId = new Integer(str);
		str = toke.nextToken();
		if("null".equals(str))
			adUserId = null;
		else
			adUserId = new Integer(str);
		str = toke.nextToken();
		if("null".equals(str))
			recordId = null;
		else
			recordId = new Integer(str);
	}

	protected static class Tokenizer {
		private final String str;
		private int last;

		public Tokenizer (String str) {
			this.str = str;
		}

		public String nextToken () {
			int next = str.indexOf("::", last);
			String part;
			if(next == -1) {
				part = str.substring(last);
				last = str.length();
			} else {
				part = str.substring(last, next);
				last = next + 2;
			}
			return part;
		}
	}
}