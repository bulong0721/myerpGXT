package org.adempiere.model;

import java.io.*;
import java.util.*;

/**
 * Application identity class for: org.adempiere.model.RRequestupdates
 *
 * Auto-generated by:
 * org.apache.openjpa.enhance.ApplicationIdTool
 */
public class RRequestupdatesPK implements Serializable {
	static {
		// register persistent class in JVM
		try { Class.forName("org.adempiere.model.RRequestupdates"); }
		catch(Exception e) {}
	}

	public Integer adUserId;
	public Integer rRequestId;

	public RRequestupdatesPK() {
	}

	public RRequestupdatesPK(String str) {
		fromString(str);
	}

	public Integer getAdUserId() {
		return adUserId;
	}

	public void setAdUserId(Integer adUserId) {
		this.adUserId = adUserId;
	}

	public Integer getRRequestId() {
		return rRequestId;
	}

	public void setRRequestId(Integer rRequestId) {
		this.rRequestId = rRequestId;
	}

	public String toString() {
		return String.valueOf(adUserId)
			+ "::" + String.valueOf(rRequestId);
	}

	public int hashCode() {
		int rs = 17;
		rs = rs * 37 + ((adUserId == null) ? 0 : adUserId.hashCode());
		rs = rs * 37 + ((rRequestId == null) ? 0 : rRequestId.hashCode());
		return rs;
	}

	public boolean equals(Object obj) {
		if(this == obj)
			return true;
		if(obj == null || obj.getClass() != getClass())
			return false;

		RRequestupdatesPK other = (RRequestupdatesPK) obj;
		return ((adUserId == null && other.adUserId == null)
			|| (adUserId != null && adUserId.equals(other.adUserId)))
			&& ((rRequestId == null && other.rRequestId == null)
			|| (rRequestId != null && rRequestId.equals(other.rRequestId)));
	}

	private void fromString(String str) {
		Tokenizer toke = new Tokenizer(str);
		str = toke.nextToken();
		if("null".equals(str))
			adUserId = null;
		else
			adUserId = new Integer(str);
		str = toke.nextToken();
		if("null".equals(str))
			rRequestId = null;
		else
			rRequestId = new Integer(str);
	}

	protected static class Tokenizer {
		private final String str;
		private int last;

		public Tokenizer (String str) {
			this.str = str;
		}

		public String nextToken () {
			int next = str.indexOf("::", last);
			String part;
			if(next == -1) {
				part = str.substring(last);
				last = str.length();
			} else {
				part = str.substring(last, next);
				last = next + 2;
			}
			return part;
		}
	}
}