package org.adempiere.model;

import java.io.*;
import java.util.*;

/**
 * Application identity class for: org.adempiere.model.ADRecordAccess
 *
 * Auto-generated by:
 * org.apache.openjpa.enhance.ApplicationIdTool
 */
public class ADRecordAccessPK implements Serializable {
	static {
		// register persistent class in JVM
		try { Class.forName("org.adempiere.model.ADRecordAccess"); }
		catch(Exception e) {}
	}

	public Integer aDRoleID;
	public Integer aDTableID;
	public Integer recordID;

	public ADRecordAccessPK() {
	}

	public ADRecordAccessPK(String str) {
		fromString(str);
	}

	public Integer getADRoleID() {
		return aDRoleID;
	}

	public void setADRoleID(Integer aDRoleID) {
		this.aDRoleID = aDRoleID;
	}

	public Integer getADTableID() {
		return aDTableID;
	}

	public void setADTableID(Integer aDTableID) {
		this.aDTableID = aDTableID;
	}

	public Integer getRecordID() {
		return recordID;
	}

	public void setRecordID(Integer recordID) {
		this.recordID = recordID;
	}

	public String toString() {
		return String.valueOf(aDRoleID)
			+ "::" + String.valueOf(aDTableID)
			+ "::" + String.valueOf(recordID);
	}

	public int hashCode() {
		int rs = 17;
		rs = rs * 37 + ((aDRoleID == null) ? 0 : aDRoleID.hashCode());
		rs = rs * 37 + ((aDTableID == null) ? 0 : aDTableID.hashCode());
		rs = rs * 37 + ((recordID == null) ? 0 : recordID.hashCode());
		return rs;
	}

	public boolean equals(Object obj) {
		if(this == obj)
			return true;
		if(obj == null || obj.getClass() != getClass())
			return false;

		ADRecordAccessPK other = (ADRecordAccessPK) obj;
		return ((aDRoleID == null && other.aDRoleID == null)
			|| (aDRoleID != null && aDRoleID.equals(other.aDRoleID)))
			&& ((aDTableID == null && other.aDTableID == null)
			|| (aDTableID != null && aDTableID.equals(other.aDTableID)))
			&& ((recordID == null && other.recordID == null)
			|| (recordID != null && recordID.equals(other.recordID)));
	}

	private void fromString(String str) {
		Tokenizer toke = new Tokenizer(str);
		str = toke.nextToken();
		if("null".equals(str))
			aDRoleID = null;
		else
			aDRoleID = new Integer(str);
		str = toke.nextToken();
		if("null".equals(str))
			aDTableID = null;
		else
			aDTableID = new Integer(str);
		str = toke.nextToken();
		if("null".equals(str))
			recordID = null;
		else
			recordID = new Integer(str);
	}

	protected static class Tokenizer {
		private final String str;
		private int last;

		public Tokenizer (String str) {
			this.str = str;
		}

		public String nextToken () {
			int next = str.indexOf("::", last);
			String part;
			if(next == -1) {
				part = str.substring(last);
				last = str.length();
			} else {
				part = str.substring(last, next);
				last = next + 2;
			}
			return part;
		}
	}
}