package org.adempiere.model;

import java.io.*;
import java.util.*;

/**
 * Application identity class for: org.adempiere.model.ADAttributeValue
 *
 * Auto-generated by:
 * org.apache.openjpa.enhance.ApplicationIdTool
 */
public class ADAttributeValuePK implements Serializable {
	static {
		// register persistent class in JVM
		try { Class.forName("org.adempiere.model.ADAttributeValue"); }
		catch(Exception e) {}
	}

	public Integer aDAttributeID;
	public Integer recordID;

	public ADAttributeValuePK() {
	}

	public ADAttributeValuePK(String str) {
		fromString(str);
	}

	public Integer getADAttributeID() {
		return aDAttributeID;
	}

	public void setADAttributeID(Integer aDAttributeID) {
		this.aDAttributeID = aDAttributeID;
	}

	public Integer getRecordID() {
		return recordID;
	}

	public void setRecordID(Integer recordID) {
		this.recordID = recordID;
	}

	public String toString() {
		return String.valueOf(aDAttributeID)
			+ "::" + String.valueOf(recordID);
	}

	public int hashCode() {
		int rs = 17;
		rs = rs * 37 + ((aDAttributeID == null) ? 0 : aDAttributeID.hashCode());
		rs = rs * 37 + ((recordID == null) ? 0 : recordID.hashCode());
		return rs;
	}

	public boolean equals(Object obj) {
		if(this == obj)
			return true;
		if(obj == null || obj.getClass() != getClass())
			return false;

		ADAttributeValuePK other = (ADAttributeValuePK) obj;
		return ((aDAttributeID == null && other.aDAttributeID == null)
			|| (aDAttributeID != null && aDAttributeID.equals(other.aDAttributeID)))
			&& ((recordID == null && other.recordID == null)
			|| (recordID != null && recordID.equals(other.recordID)));
	}

	private void fromString(String str) {
		Tokenizer toke = new Tokenizer(str);
		str = toke.nextToken();
		if("null".equals(str))
			aDAttributeID = null;
		else
			aDAttributeID = new Integer(str);
		str = toke.nextToken();
		if("null".equals(str))
			recordID = null;
		else
			recordID = new Integer(str);
	}

	protected static class Tokenizer {
		private final String str;
		private int last;

		public Tokenizer (String str) {
			this.str = str;
		}

		public String nextToken () {
			int next = str.indexOf("::", last);
			String part;
			if(next == -1) {
				part = str.substring(last);
				last = str.length();
			} else {
				part = str.substring(last, next);
				last = next + 2;
			}
			return part;
		}
	}
}