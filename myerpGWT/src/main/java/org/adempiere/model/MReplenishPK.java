package org.adempiere.model;

import java.io.*;
import java.util.*;

/**
 * Application identity class for: org.adempiere.model.MReplenish
 *
 * Auto-generated by:
 * org.apache.openjpa.enhance.ApplicationIdTool
 */
public class MReplenishPK implements Serializable {
	static {
		// register persistent class in JVM
		try { Class.forName("org.adempiere.model.MReplenish"); }
		catch(Exception e) {}
	}

	public Integer mProductID;
	public Integer mWarehouseID;

	public MReplenishPK() {
	}

	public MReplenishPK(String str) {
		fromString(str);
	}

	public Integer getMProductID() {
		return mProductID;
	}

	public void setMProductID(Integer mProductID) {
		this.mProductID = mProductID;
	}

	public Integer getMWarehouseID() {
		return mWarehouseID;
	}

	public void setMWarehouseID(Integer mWarehouseID) {
		this.mWarehouseID = mWarehouseID;
	}

	public String toString() {
		return String.valueOf(mProductID)
			+ "::" + String.valueOf(mWarehouseID);
	}

	public int hashCode() {
		int rs = 17;
		rs = rs * 37 + ((mProductID == null) ? 0 : mProductID.hashCode());
		rs = rs * 37 + ((mWarehouseID == null) ? 0 : mWarehouseID.hashCode());
		return rs;
	}

	public boolean equals(Object obj) {
		if(this == obj)
			return true;
		if(obj == null || obj.getClass() != getClass())
			return false;

		MReplenishPK other = (MReplenishPK) obj;
		return ((mProductID == null && other.mProductID == null)
			|| (mProductID != null && mProductID.equals(other.mProductID)))
			&& ((mWarehouseID == null && other.mWarehouseID == null)
			|| (mWarehouseID != null && mWarehouseID.equals(other.mWarehouseID)));
	}

	private void fromString(String str) {
		Tokenizer toke = new Tokenizer(str);
		str = toke.nextToken();
		if("null".equals(str))
			mProductID = null;
		else
			mProductID = new Integer(str);
		str = toke.nextToken();
		if("null".equals(str))
			mWarehouseID = null;
		else
			mWarehouseID = new Integer(str);
	}

	protected static class Tokenizer {
		private final String str;
		private int last;

		public Tokenizer (String str) {
			this.str = str;
		}

		public String nextToken () {
			int next = str.indexOf("::", last);
			String part;
			if(next == -1) {
				part = str.substring(last);
				last = str.length();
			} else {
				part = str.substring(last, next);
				last = next + 2;
			}
			return part;
		}
	}
}