package org.adempiere.model;

import java.io.*;
import java.util.*;

/**
 * Application identity class for: org.adempiere.model.CBpartnerProduct
 *
 * Auto-generated by:
 * org.apache.openjpa.enhance.ApplicationIdTool
 */
public class CBpartnerProductPK implements Serializable {
	static {
		// register persistent class in JVM
		try { Class.forName("org.adempiere.model.CBpartnerProduct"); }
		catch(Exception e) {}
	}

	public Integer cBpartnerId;
	public Integer mProductId;

	public CBpartnerProductPK() {
	}

	public CBpartnerProductPK(String str) {
		fromString(str);
	}

	public Integer getCBpartnerId() {
		return cBpartnerId;
	}

	public void setCBpartnerId(Integer cBpartnerId) {
		this.cBpartnerId = cBpartnerId;
	}

	public Integer getMProductId() {
		return mProductId;
	}

	public void setMProductId(Integer mProductId) {
		this.mProductId = mProductId;
	}

	public String toString() {
		return String.valueOf(cBpartnerId)
			+ "::" + String.valueOf(mProductId);
	}

	public int hashCode() {
		int rs = 17;
		rs = rs * 37 + ((cBpartnerId == null) ? 0 : cBpartnerId.hashCode());
		rs = rs * 37 + ((mProductId == null) ? 0 : mProductId.hashCode());
		return rs;
	}

	public boolean equals(Object obj) {
		if(this == obj)
			return true;
		if(obj == null || obj.getClass() != getClass())
			return false;

		CBpartnerProductPK other = (CBpartnerProductPK) obj;
		return ((cBpartnerId == null && other.cBpartnerId == null)
			|| (cBpartnerId != null && cBpartnerId.equals(other.cBpartnerId)))
			&& ((mProductId == null && other.mProductId == null)
			|| (mProductId != null && mProductId.equals(other.mProductId)));
	}

	private void fromString(String str) {
		Tokenizer toke = new Tokenizer(str);
		str = toke.nextToken();
		if("null".equals(str))
			cBpartnerId = null;
		else
			cBpartnerId = new Integer(str);
		str = toke.nextToken();
		if("null".equals(str))
			mProductId = null;
		else
			mProductId = new Integer(str);
	}

	protected static class Tokenizer {
		private final String str;
		private int last;

		public Tokenizer (String str) {
			this.str = str;
		}

		public String nextToken () {
			int next = str.indexOf("::", last);
			String part;
			if(next == -1) {
				part = str.substring(last);
				last = str.length();
			} else {
				part = str.substring(last, next);
				last = next + 2;
			}
			return part;
		}
	}
}