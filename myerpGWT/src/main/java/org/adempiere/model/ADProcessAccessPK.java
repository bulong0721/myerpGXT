package org.adempiere.model;

import java.io.*;
import java.util.*;

/**
 * Application identity class for: org.adempiere.model.ADProcessAccess
 *
 * Auto-generated by:
 * org.apache.openjpa.enhance.ApplicationIdTool
 */
public class ADProcessAccessPK implements Serializable {
	static {
		// register persistent class in JVM
		try { Class.forName("org.adempiere.model.ADProcessAccess"); }
		catch(Exception e) {}
	}

	public Integer aDProcessID;
	public Integer aDRoleID;

	public ADProcessAccessPK() {
	}

	public ADProcessAccessPK(String str) {
		fromString(str);
	}

	public Integer getADProcessID() {
		return aDProcessID;
	}

	public void setADProcessID(Integer aDProcessID) {
		this.aDProcessID = aDProcessID;
	}

	public Integer getADRoleID() {
		return aDRoleID;
	}

	public void setADRoleID(Integer aDRoleID) {
		this.aDRoleID = aDRoleID;
	}

	public String toString() {
		return String.valueOf(aDProcessID)
			+ "::" + String.valueOf(aDRoleID);
	}

	public int hashCode() {
		int rs = 17;
		rs = rs * 37 + ((aDProcessID == null) ? 0 : aDProcessID.hashCode());
		rs = rs * 37 + ((aDRoleID == null) ? 0 : aDRoleID.hashCode());
		return rs;
	}

	public boolean equals(Object obj) {
		if(this == obj)
			return true;
		if(obj == null || obj.getClass() != getClass())
			return false;

		ADProcessAccessPK other = (ADProcessAccessPK) obj;
		return ((aDProcessID == null && other.aDProcessID == null)
			|| (aDProcessID != null && aDProcessID.equals(other.aDProcessID)))
			&& ((aDRoleID == null && other.aDRoleID == null)
			|| (aDRoleID != null && aDRoleID.equals(other.aDRoleID)));
	}

	private void fromString(String str) {
		Tokenizer toke = new Tokenizer(str);
		str = toke.nextToken();
		if("null".equals(str))
			aDProcessID = null;
		else
			aDProcessID = new Integer(str);
		str = toke.nextToken();
		if("null".equals(str))
			aDRoleID = null;
		else
			aDRoleID = new Integer(str);
	}

	protected static class Tokenizer {
		private final String str;
		private int last;

		public Tokenizer (String str) {
			this.str = str;
		}

		public String nextToken () {
			int next = str.indexOf("::", last);
			String part;
			if(next == -1) {
				part = str.substring(last);
				last = str.length();
			} else {
				part = str.substring(last, next);
				last = next + 2;
			}
			return part;
		}
	}
}