package org.adempiere.model;

import java.io.*;
import java.util.*;

/**
 * Application identity class for: org.adempiere.model.TSelection
 *
 * Auto-generated by:
 * org.apache.openjpa.enhance.ApplicationIdTool
 */
public class TSelectionPK implements Serializable {
	static {
		// register persistent class in JVM
		try { Class.forName("org.adempiere.model.TSelection"); }
		catch(Exception e) {}
	}

	public Integer adPinstanceId;
	public Integer tSelectionId;

	public TSelectionPK() {
	}

	public TSelectionPK(String str) {
		fromString(str);
	}

	public Integer getAdPinstanceId() {
		return adPinstanceId;
	}

	public void setAdPinstanceId(Integer adPinstanceId) {
		this.adPinstanceId = adPinstanceId;
	}

	public Integer getTSelectionId() {
		return tSelectionId;
	}

	public void setTSelectionId(Integer tSelectionId) {
		this.tSelectionId = tSelectionId;
	}

	public String toString() {
		return String.valueOf(adPinstanceId)
			+ "::" + String.valueOf(tSelectionId);
	}

	public int hashCode() {
		int rs = 17;
		rs = rs * 37 + ((adPinstanceId == null) ? 0 : adPinstanceId.hashCode());
		rs = rs * 37 + ((tSelectionId == null) ? 0 : tSelectionId.hashCode());
		return rs;
	}

	public boolean equals(Object obj) {
		if(this == obj)
			return true;
		if(obj == null || obj.getClass() != getClass())
			return false;

		TSelectionPK other = (TSelectionPK) obj;
		return ((adPinstanceId == null && other.adPinstanceId == null)
			|| (adPinstanceId != null && adPinstanceId.equals(other.adPinstanceId)))
			&& ((tSelectionId == null && other.tSelectionId == null)
			|| (tSelectionId != null && tSelectionId.equals(other.tSelectionId)));
	}

	private void fromString(String str) {
		Tokenizer toke = new Tokenizer(str);
		str = toke.nextToken();
		if("null".equals(str))
			adPinstanceId = null;
		else
			adPinstanceId = new Integer(str);
		str = toke.nextToken();
		if("null".equals(str))
			tSelectionId = null;
		else
			tSelectionId = new Integer(str);
	}

	protected static class Tokenizer {
		private final String str;
		private int last;

		public Tokenizer (String str) {
			this.str = str;
		}

		public String nextToken () {
			int next = str.indexOf("::", last);
			String part;
			if(next == -1) {
				part = str.substring(last);
				last = str.length();
			} else {
				part = str.substring(last, next);
				last = next + 2;
			}
			return part;
		}
	}
}